---
import { Code } from "astro:components"
import { SHIKI_THEMES } from "../lib/themes"

interface Props {
  code: string
  lang?: string
}

const { code, lang = "ts" } = Astro.props
---

<Code
  code={code}
  lang={lang as "ts"}
  themes={SHIKI_THEMES}
  defaultColor={false}
  wrap
/>
